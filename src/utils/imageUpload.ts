// Simple image upload helper (placeholder)
export async function compressAndUploadImage(uri: string){
  // Placeholder: use expo-image-manipulator in the app to resize/compress before upload
  // Then upload to Supabase Storage at {user_id}/{plant_id}/{photo_id}.jpg
  return { path: uri };
}
